import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  root: {
    flex: 1,
    backgroundColor: "#fff",
    paddingLeft: 12,
    paddingRight: 12
  },
});

export default styles;
